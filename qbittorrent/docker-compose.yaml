services:
  qbittorrent:
    container_name: qbittorrent
    image: linuxserver/qbittorrent-nox:latest
    networks:
      - qbit-network
      - proxy-network
    volumes:
      - ./.config:/config
      - /mnt/seagate-alpha/torrents:/downloads/seagate-alpha
      - /mnt/seagate-beta/torrents:/downloads/seagate-beta
networks:
  qbit-network:
    external: true
  proxy-network:
    external: true